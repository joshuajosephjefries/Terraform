# Variables that prompt for user input

variable "count_no" {
    # This block is left empty without variables for user input prompt. Expecting Type -> number. 
    # Since we did not mention any type, it defaultly considers type as "all".
}
variable "country" {
    # This block is left empty without variables for user input prompt. Expecting Type -> string
    # Since we did not mention any type, it defaultly considers type as "all".
}